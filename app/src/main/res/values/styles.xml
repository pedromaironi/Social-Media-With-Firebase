<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditTextWhite" parent="TextAppearance.AppCompat">
        <item name="android:textColorHint">@color/primary_black_color</item>
        <item name="android:textColorPrimary">@color/primary_black_color</item>
        <item name="android:colorAccent">@color/primary_black_color</item>
        <item name="android:colorControlNormal">@color/primary_black_color</item>
        <item name="android:colorControlActivated">@color/primary_black_color</item>
    </style>

</resources>